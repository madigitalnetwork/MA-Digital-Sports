# Copy to .env.local for local development, and set the same key in the
# Vercel dashboard under Settings -> Environment Variables.
CRIC_API_KEY=your_key_here

# Optional: pin the scoreboard to one specific match id instead of
# whichever match happens to be live.
# MATCH_ID=
